server {
    listen 8080;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This is the magic line for React/Vite apps:
        # If a file isn't found, serve index.html so React can handle the routing.
        try_files $uri $uri/ /index.html;
    }
}
